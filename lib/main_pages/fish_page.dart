import 'package:flutter/material.dart';

class FishPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Fish Content Goes Here',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}